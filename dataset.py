from torch.utils.data import Dataset
import numpy as np
import torch

class Mapdataset(Dataset):

    def __init__(self,basedir, numTrainingSamples, numValidationSamples, numTestSamples, gpu=True):
        print("Hello")

    def __getitem__(self, index):
        return 

    def __len__(self):
        return 0